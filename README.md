# KMeans-GPU-Benchmark
A high-performance benchmark of the K-Means clustering algorithm, comparing CPU execution (scikit-learn) against GPU acceleration using RAPIDS cuML and a custom C++/CUDA kernel.
